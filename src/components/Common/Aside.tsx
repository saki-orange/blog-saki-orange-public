export default function Aside({ children }: { children: React.ReactNode }) {
  // return <aside className="hidden md:block md:w-[23%] pb-[250px]">{children}</aside>;
  return <aside className="hidden md:block md:w-[250px] pb-[250px]">{children}</aside>;
}
